# Tanks-IO frontend repository

### Written in `React` with `TypeScript`. Uses `SignalR` for real time server-client communication

### Backend is now in a [separate repository](https://github.com/ArtemZan/tanks-io-backend)